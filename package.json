{
  "name": "nodaysidle-project-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start",
    "vercel:prepare": "npm run build && node scripts/prepare-vercel-output.mjs",
    "deploy": "npm run vercel:prepare && vercel deploy --prebuilt --prod"
  },
  "dependencies": {
    "next": "15.4.10",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
